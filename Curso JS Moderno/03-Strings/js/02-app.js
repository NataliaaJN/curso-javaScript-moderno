//      STRING METHODS     //
const product = "Monitor de 20 pulgadas"; 

console.log(product.length);

console.log(product.indexOf("Monitor")); // devuelve 0
console.log(product.includes("Monitor")); // devuelve true