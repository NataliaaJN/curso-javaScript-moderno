//          OBJECT  DESTRUCTURING      //

const product = {
    name: "Monitor 20 pulgadas",
    price: 300,
    available: true,
}

const {name, price} = product;
console.log(name);
console.log(price);

