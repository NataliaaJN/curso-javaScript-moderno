//         THIS         //

const product = {
    name : "Monitor 20 pulgadas",
    price : 300,
    available : true,
    showInfo: function() {
        console.log(`El producto: ${name} tiene un precio de: ${price}`);
    }
}