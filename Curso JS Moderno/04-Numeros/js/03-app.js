//       OBJECT MATH        //

let result;

// PI
result= Math.PI;

// Redondear
result = Math.round(2.8);
result = Math.floor(2.2);
result = Math.floor(2.5); // redondea hacia abajo

// Redondear hacia arriba
result = Math.ceil(2.1)  // redondea a 2

// Redondeer hacia abajo
result = Math.floor(2.9) // redondea a 1

// Raíz cuadrada
result = Math.sqrt(144);

// Valor absoluto 
result = Math.abs(-500); // 500

// Potencia
result = Math.pow(2,4);

// Mínimo
result = Math.min(3,5,1); // Devuelve el mínimo: 1

// Máximo 
result = Math.max(2,5,-3,12,10); // Devuelve el máximo: 12

// Aleatorio
result = Math.random() * 20; // Número aleatorio decimal

result = Math.floor(Math.random() * 30);